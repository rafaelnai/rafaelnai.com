import styled from "styled-components";

const Styled = {
  Header: styled.header`
    display: flex;
    flex-direction: column;
  `,
  Title: styled.h1`
    font-size: var(--title);
    text-align: center;
    font-weight: bold;
  `,
};

export default Styled;
